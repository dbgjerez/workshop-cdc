apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Values.service.name }}
  annotations:
    argocd.argoproj.io/hook: PreSync
spec:
  template:
    spec:
      containers:
      - name: pre-sync
        image: quay.io/openshift/origin-cli:latest
        command: ["sh", "-c"]
        args:
          - >
            URL=$(oc get route ms-users-camel -o jsonpath='{.spec.host}');
            oc create configmap backend-url --from-literal=url=$URL --dry-run=client -o yaml | oc apply -f -
      restartPolicy: Never
